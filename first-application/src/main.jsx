import React from "react";
import ReactDOM from "react-dom/client";
const myFirstele = <h1>first pro</h1>; //jsx// to display is in root du
ReactDOM.createRoot(document.getElementById("root")).render(myFirstele);
