console.log(isNaN('Hello'));//returns true if value is is NaN
console.log(Number.isNaN('Hello'));//returns true only if the value of the arguement is NaN and of the type number.
console.log(isNaN(NaN));
console.log(Number.isNaN(NaN));
